---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: virtualdisks.cloudhypervisor.quill.today
spec:
  group: cloudhypervisor.quill.today
  names:
    kind: VirtualDisk
    listKind: VirtualDiskList
    plural: virtualdisks
    shortNames:
    - vdisk
    singular: virtualdisk
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: VirtualDisk is the Schema for the virtualdisks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualDiskSpec defines the desired state of VirtualDisk
            properties:
              source:
                description: Source configuration
                properties:
                  diskSnapshot:
                    properties:
                      snapshotName:
                        type: string
                    required:
                    - snapshotName
                    type: object
                  empty:
                    type: object
                  http:
                    description: HTTP source
                    properties:
                      url:
                        description: HTTP URL
                        type: string
                    required:
                    - url
                    type: object
                  registry:
                    description: Registry source
                    properties:
                      pullMethod:
                        description: Pull method
                        type: string
                      url:
                        description: Registry URL
                        type: string
                    required:
                    - url
                    type: object
                type: object
              storage:
                description: Storage configuration
                properties:
                  accessModes:
                    description: Access modes for the disk
                    items:
                      type: string
                    type: array
                  resources:
                    description: Storage resources
                    properties:
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Storage requests
                        type: object
                    type: object
                  volumeMode:
                    description: Volume mode (Filesystem or Block)
                    type: string
                type: object
            type: object
          status:
            description: VirtualDiskStatus defines the observed state of VirtualDisk
            properties:
              attached:
                description: Whether the disk is attached
                type: boolean
              attachedTo:
                description: Name of the VM the disk is attached to
                type: string
              available:
                anyOf:
                - type: integer
                - type: string
                description: Available space on the disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              capacity:
                anyOf:
                - type: integer
                - type: string
                description: Capacity of the disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              conditions:
                description: Conditions for the disk
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              path:
                description: Disk path
                type: string
              phase:
                description: Phase of the disk
                enum:
                - Pending
                - Bound
                - Ready
                - Failed
                - Terminating
                type: string
              used:
                anyOf:
                - type: integer
                - type: string
                description: Used space on the disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
